# Support Contact Pre-Submit Asset

Required before submission:

- final support URL: `https://lucky-me.app/support`;
- support email or contact channel for the store listing;
- Publisher Portal contact address controlled by the publisher.

The app production validator requires `EXPO_PUBLIC_LUCKYME_SUPPORT_URL` to be a
final HTTPS URL. The configured public domain is `lucky-me.app`.
